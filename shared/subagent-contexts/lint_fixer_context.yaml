identity:
  agentId: 'lint_fixer'
  taskId: 'subagent_1752522834274_lint_fixer'
  agentRole: 'Run linting fixes'
  focusArea: 'code_fixes'
  taskSource: 'LINTING-FIXES-CHECKLIST.md'
execution:
  canStartImmediately: false
  dependencies:
    - 'typeof_fixer'
    - 'batch_fixer'
    - 'file_fixer'
  estimatedTime: 10
deliverables:
  tasks:
    - 'Run npm run lint:fix after manual changes'
    - 'Run npm run lint:fix for auto-fixable issues'
  validationCriteria:
    - 'Complete: Run npm run lint:fix after manual changes'
    - 'Complete: Run npm run lint:fix for auto-fixable issues'
  successMetrics:
    - 'All code_fixes tasks completed successfully'
orchestration:
  phase: 2
  concurrentWith: []
